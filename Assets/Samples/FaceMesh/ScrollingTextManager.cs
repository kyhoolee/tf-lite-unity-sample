using System.Collections;
using UnityEngine;
using UnityEngine.UI;
public class ScrollingTextManager : MonoBehaviour
{   
    public RectTransform comp;
    // public Text textMesh;
    // Start is called before the first frame update
    IEnumerator Start()
    {   
        // float width ;
        // Vector3 startPosition = comp.position;

        // float scrollPosition = 0;
        // while(true){
        //     comp.position = new Vector3(-scrollPosition*10, startPosition.y, startPosition.z);
        //     scrollPosition += 20 * 20 * Time.deltaTime;

            yield return null;
        // }
    }
}
